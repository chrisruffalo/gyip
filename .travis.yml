language: go

go:
- '1.10'

matrix:
  include:
  - OS_TYPE=fedora OS_VERSION=28

git:
  depth: 1

notifications:
  email: false

script:
- go test -v -race ./...

before_deploy:
 - dnf install -y git buildah runc podman

deploy:
  on:
    branch: release
  provider: script
  script: bash deploy.sh
